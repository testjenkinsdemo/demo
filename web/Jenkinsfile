pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        echo 'Checkout code'
      }
    }
        stage('Web') {
          steps {
            bat 'runWebTests.bat'
          }
        }
  }
  post {
    always {
      junit 'results/*.xml'

    }

  }
}
